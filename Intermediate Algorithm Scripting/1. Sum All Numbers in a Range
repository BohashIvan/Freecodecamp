function sumAll(arr) {
	var sorted = arr.sort(function (a, b) {
		return a - b;
	});
	var count = 0;

	for (var i = sorted[0]; i <= sorted[1]; i += 1) {
		count += i;
	}

	return count;
}

sumAll([4, 1]);
sumAll([5, 10]);
